[sync]
	lastCommit = eefd84403f1a81407e48a49502127e99583f9166
	# v2.3.0 — what the dashboard vendored on 2026-05-08.
	# The upstream-watch action diffs upstream/main against this and opens a
	# weekly tracking issue with new commits. After review + cherry-pick (into
	# agenticapps-dashboard/packages/agentlinter/src/), advance this to the
	# new upstream/main HEAD via:
	#   git config --file .upstream-sync.cfg sync.lastCommit "$(git rev-parse upstream/main)"
	#   git commit .upstream-sync.cfg -m "chore: advance upstream-sync marker"
